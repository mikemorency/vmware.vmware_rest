# README FIRST
# 1. Subscribe to https://github.com/ansible-collections/news-for-maintainers
#    (click the Watch button on the homepage > Custom > Issues)
#    and keep this matrix up to date in accordance to related announcements.
#    Timely add new ansible-core versions and consider dropping support
#    and testing against its EOL versions.
# 2. If your collection repository is under the ansible-collections org,
#    please keep in mind that the number of GHA jobs is limited
#    and shared across all the collections in the org.
#    So, focusing on good test coverage of your collection,
#    please avoid testing against unnecessary entities such as
#    ansible-core EOL versions your collection does not support
#    or ansible-core versions that are not EOL yet but not supported by the collection.
# 3. If you don't have unit or integration tests, remove corresponding sections.
# 4. If your collection depends on other collections ensure they are installed,
#    add them to the "test-deps" input.
# 5. For the comprehensive list of the inputs supported by the
#    ansible-community/ansible-test-gh-action GitHub Action, see
#    https://github.com/marketplace/actions/ansible-test.
# 6. If you want to prevent merging PRs that do not pass all tests,
#    make sure to add the "check" job to your repository branch
#    protection once this workflow is added.
#    It is also possible to tweak which jobs are allowed to fail. See
#    https://github.com/marketplace/actions/alls-green#gotchas for more detail.
# 7. If you need help please ask in #community:ansible.com on Matrix
#    or in bridged #ansible-community on the Libera.Chat IRC channel.
#    See https://docs.ansible.com/ansible/devel/community/communication.html
#    for details.
# 8. If your collection is [going to get] included in the Ansible package,
#    it has to adhere to Python compatibility and CI testing requirements described in
#    https://docs.ansible.com/ansible/latest/community/collection_contributors/collection_requirements.html.

name: Integration
on:
  # Run CI against all pushes (direct commits, also merged PRs), Pull Requests
  push:
    branches:
      - main
      - stable-*
  pull_request:

concurrency:
  group: >-
    ${{ github.workflow }}-${{
      github.event.pull_request.number || github.sha
    }}
  cancel-in-progress: true

jobs:
  integration-matrix:
    name: Build integration target matrix
    runs-on: ubuntu-latest
    outputs:
      target_groups: ${{ steps.set-matrix.outputs.target_groups }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@v7
        with:
          # Full history so we can diff against the base/previous commit.
          fetch-depth: 0

      - id: changed-files
        name: Determine changed files
        run: |
          set -euo pipefail
          run_all=false
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            # Compare the whole PR against its base branch (usually main), not
            # just the latest commit, so every change on the branch is covered.
            base="${{ github.event.pull_request.base.sha }}"
            head="${{ github.event.pull_request.head.sha }}"
          else
            # For pushes, compare the branch against main rather than the
            # previous commit so the full set of changes is considered.
            head="${{ github.sha }}"
            if git rev-parse --verify --quiet origin/main >/dev/null; then
              base="$(git merge-base origin/main "$head")"
            else
              base=""
            fi
          fi
          if [ -n "$base" ] && git cat-file -e "${base}^{commit}" 2>/dev/null; then
            git diff --name-only "$base" "$head" > changed_files.txt
          else
            # Missing base/history: fall back to running everything.
            run_all=true
            : > changed_files.txt
          fi
          echo "run_all=${run_all}" >> "$GITHUB_OUTPUT"
          echo "Changed files:"
          cat changed_files.txt

      - id: set-matrix
        name: Select affected integration targets and split into groups of 10
        env:
          RUN_ALL: ${{ steps.changed-files.outputs.run_all }}
        run: |
          python <<'EOF'
          import json
          import os
          from pathlib import Path

          group_size = 10
          targets_dir = Path("tests/integration/targets")
          all_targets = sorted(
              path.name
              for path in targets_dir.iterdir()
              if path.is_dir()
              and not path.name.startswith("vmware_rest_")
              and not path.name.startswith("prepare_")
          )
          all_targets_set = set(all_targets)

          changed_path = Path("changed_files.txt")
          changed = []
          if changed_path.exists():
              changed = [
                  line.strip()
                  for line in changed_path.read_text(encoding="utf-8").splitlines()
                  if line.strip()
              ]

          def triggers_full_run(files):
              """Some changes affect everything, so all targets must run."""
              for path in files:
                  parts = path.split("/")
                  # Shared module utilities are used by every module.
                  if path.startswith("plugins/module_utils/"):
                      return True
                  # Anything under .github/ (recursive).
                  if path.startswith(".github/"):
                      return True
                  # Files directly under tests/ (non-recursive).
                  if len(parts) == 2 and parts[0] == "tests":
                      return True
                  # Files at the project root (non-recursive).
                  if len(parts) == 1:
                      return True
              return False

          run_all = os.environ.get("RUN_ALL") == "true"
          if run_all or triggers_full_run(changed):
              targets = list(all_targets)
          else:
              selected = set()
              for path in changed:
                  parts = path.split("/")
                  # A changed module maps to its matching target, e.g.
                  # plugins/modules/appliance_health_database_info.py
                  #   -> appliance_health_database
                  if path.startswith("plugins/modules/") and path.endswith(".py"):
                      stem = Path(path).stem
                      candidates = {stem}
                      if stem.endswith("_info"):
                          candidates.add(stem[: -len("_info")])
                      selected.update(candidates & all_targets_set)
                  # A changed integration target runs itself, e.g.
                  # tests/integration/targets/<target>/...
                  elif path.startswith("tests/integration/targets/") and len(parts) >= 4:
                      target = parts[3]
                      if target in all_targets_set:
                          selected.add(target)
              targets = sorted(selected)

          target_groups = [
              {
                  "id": (index // group_size) + 1,
                  "targets": " ".join(targets[index : index + group_size]),
              }
              for index in range(0, len(targets), group_size)
          ]

          print(f"Selected {len(targets)} target(s) in {len(target_groups)} group(s).")

          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as output:
              output.write(f"target_groups={json.dumps(target_groups)}\n")
          EOF

  integration:
    name: Integration (Ⓐ${{ matrix.versions.ansible }} - ${{ matrix.versions.python }} - group ${{ matrix.target_group.id }})
    needs: integration-matrix
    strategy:
      fail-fast: false
      matrix:
        target_group: ${{ fromJSON(needs.integration-matrix.outputs.target_groups) }}
        versions:
          # Testing all ansible-core and controller Python versions is impractical. Only test the
          # newest and oldest controller Python versions supported by each ansible-core we support.
          # https://endoflife.date/ansible-core (Control node Python column)
          # Add publish_coverage: true to the version you want to publish coverage for, usually latest.
          - { python: "3.14", ansible: stable-2.21 }
          - { python: "3.12", ansible: stable-2.21 }
          - { python: "3.14", ansible: stable-2.20, publish_coverage: true }
          - { python: "3.12", ansible: stable-2.20 }
          - { python: "3.13", ansible: stable-2.19 }
          - { python: "3.11", ansible: stable-2.19 }
          - { python: "3.12", ansible: stable-2.16 }
          - { python: "3.10", ansible: stable-2.16 }

    runs-on: ubuntu-latest
    steps:
      - name: Update podman
        run: |
          set -euxo pipefail
          sudo apt-get update -y
          sudo apt-get install -y --upgrade podman crun

      - name: Checkout repo
        uses: actions/checkout@v7

      - name: Install python
        uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.versions.python }}

      - name: Install ansible-core
        run: |
          python -m pip install https://github.com/ansible/ansible/archive/${{ matrix.versions.ansible }}.tar.gz;

      - name: Install collection and python requirements
        run: |
          ansible-galaxy collection install . --force;

      - name: Perform integration tests
        run: |
          make integration INTEGRATION_TARGETS="${{ matrix.target_group.targets }}";

  # This job does nothing and is only used for the branch protection
  # or multi-stage CI jobs, like making sure that all tests pass before
  # a publishing job is started.
  integration-check:
    if: always()
    needs:
      - integration
    runs-on: ubuntu-latest
    steps:
      - name: Decide whether the needed jobs succeeded or failed
        uses: re-actors/alls-green@release/v1
        with:
          # When no targets are affected by a change the integration job has an
          # empty matrix and is skipped; that must not fail the check.
          allowed-skips: integration
          jobs: ${{ toJSON(needs) }}
