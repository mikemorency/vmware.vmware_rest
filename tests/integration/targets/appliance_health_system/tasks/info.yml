---
# ============================================================
# APPLIANCE_HEALTH_SYSTEM_INFO TESTS
# ============================================================

# Test 1: Get health status when the subsystem is healthy (green)
- name: Get System health status (healthy)
  vmware.vmware_rest.appliance_health_system_info: {}
  register: system_health_green

- name: Assert healthy status is reported
  ansible.builtin.assert:
    that:
      - system_health_green.value is defined
      - system_health_green.value == 'green'
      - system_health_green.info is defined
      - system_health_green.info | length == 1
      - system_health_green.info[0] == 'green'
      - not system_health_green.changed

# Test 2: Get health status when the subsystem is degraded (orange)
- name: Reset before degraded health expectations
  ansible.builtin.include_role:
    name: prepare_simulator
  vars:
    prepare_simulator_reset: true

- name: Load API spec expectations for degraded health
  ansible.builtin.uri:
    url: "{{ mockserver_url }}/mockserver/openapi"
    method: PUT
    status_code: [200, 201]
    body_format: json
    body:
      specUrlOrPayload: "file:/mockserver_specs/degraded.json"
      operationsAndResponses:
        Appliance.Health.System_get: "200"

- name: Get System health status (degraded)
  vmware.vmware_rest.appliance_health_system_info: {}
  register: system_health_orange

- name: Assert degraded status is reported
  ansible.builtin.assert:
    that:
      - system_health_orange.value == 'orange'
      - system_health_orange.info | length == 1
      - system_health_orange.info[0] == 'orange'
      - not system_health_orange.changed

# Test 3: Check mode returns the same read-only result without error
- name: Get System health status (check mode)
  vmware.vmware_rest.appliance_health_system_info: {}
  check_mode: true
  register: system_health_check

- name: Assert check mode reports no change for read-only info module
  ansible.builtin.assert:
    that:
      - system_health_check.value == 'orange'
      - not system_health_check.changed
